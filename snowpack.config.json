{
  "scripts": {
    "mount:public": "mount public --to /",
    "mount:src": "mount src --to /_dist_"
  },
  "plugins": ["@snowpack/plugin-babel"],
  "installOptions": {
    "installTypes": true
  }
}
